<UserControl x:Class="Muxx.UI.Controls.DisplayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MaxWidth="400"
             Height="auto" 
             Width="auto">

   <StackPanel>

      <TextBlock Name="txbMensagem" 
                 VerticalAlignment="Stretch" 
                 HorizontalAlignment="Stretch" 
                 xml:space="preserve" 
                 Margin="10,10,10,0"><LineBreak/>   PROCESSANDO...   <LineBreak/></TextBlock>

      <ProgressBar Name="pgbBar" 
                   HorizontalAlignment="Stretch"
                   Height="50" 
                   Width="auto" 
                   Margin="10,0,10,10"
                   IsIndeterminate="True" />
   </StackPanel>

</UserControl>